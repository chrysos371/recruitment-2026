# Software_C3 — 红绿灯检测（传统计算机视觉）

## 自我介绍

我是张杨亦航（学号 2524030231）。这道题是计算机视觉方向的压轴题（5 分），要求用传统 CV 方法（不用深度学习）对红绿灯进行鲁棒检测。相比 C1（Haar Cascade 人脸检测）和 C2（YOLO 深度学习），C3 回归到最基础的颜色分割 + 形态学 + 轮廓分析的组合拳——在这些底层操作上应对各种干扰（反光、夜灯、尾灯、广告牌），非常考验 CV 基本功。

---

## 算法设计

### 整体流程

```
输入图像
  ↓ BGR → HSV 颜色空间转换
  ↓
为 红/黄/绿 各创建颜色掩码
  ├─ 红色: 两段 HSV 范围 (跨越 0° 分界线)
  ├─ 黄色: 一段 HSV 范围
  └─ 绿色: 一段 HSV 范围
  ↓
暗光场景: 降低 V 阈值重新检测
  ↓
形态学处理
  ├─ 开运算 (Open): 去除小噪点
  └─ 闭运算 (Close): 填充孔洞, 连接断裂
  ↓
轮廓检测 + 几何过滤
  ├─ 面积: 20-5000 px² (滤噪点+广告牌)
  ├─ 圆形度: > 0.3 (滤不规则形状)
  ├─ 长宽比: < 3.0 (滤长条形)
  └─ 位置: 底部 30% 降权 (滤尾灯)
  ↓
输出: 信号灯状态 + 绘制检测框
```

### 鲁棒性设计

| 干扰场景 | 应对方案 |
|----------|----------|
| **阳光反光** | HSV 饱和度下限 150（白色反光饱和度低，被滤除）|
| **夜晚灯光** | 暗通道检测：图像平均灰度 < 80 时降低 V 阈值 |
| **红色尾灯** | 位置过滤：图像底部 30% 区域置信度 ×0.5 |
| **绿色广告牌** | 面积上限 5000 + 圆形度下限 0.3（广告牌大且不规则）|
| **摄像头抖动** | 闭运算连接因抖动导致的断裂区域 |

### HSV 颜色范围

```
红色: H∈[0,10]∪[160,180], S∈[150,255], V∈[80,255]
黄色: H∈[18,33],  S∈[120,255], V∈[120,255]
绿色: H∈[45,85],  S∈[100,255], V∈[80,255]
```

---

## 使用方法

```bash
cd Software_C3
python traffic_light_detector.py                  # 处理所有 40 张图片
python traffic_light_detector.py RGBlight1.jpg    # 处理单张图片
```

输出: `output/detected_*.jpg` — 带检测框和状态标注的图片。

---

## 文件结构

```
Software_C3/
├── RGBlight*.jpg                  # 原始图片 (40张)
├── traffic_light_detector.py      # 红绿灯检测主程序
├── output/                        # 检测结果图片
├── README.md
└── notes.md
```

---

## 技术要点

| 技术 | 函数/方法 | 作用 |
|------|-----------|------|
| 颜色空间 | `cv2.cvtColor(BGR2HSV)` | HSV 分离色调/饱和度/明度 |
| 阈值分割 | `cv2.inRange()` | 创建二值颜色掩码 |
| 形态学 | `cv2.morphologyEx(OPEN/CLOSE)` | 去噪+连接 |
| 轮廓分析 | `cv2.findContours()` | 提取连通区域 |
| 圆形度 | `4πA/P²` | 红绿灯是圆形 |

---

## 局限性（诚实交代）

- 传统 CV 方法无法完美区分红色尾灯和红色信号灯（仅靠位置启发式）
- 极端光照（强逆光）下 HSV 饱和度可能不足以区分
- 复杂城市场景中绿色植被可能被误检
- 对于题目要求的鲁棒性场景（反光/夜灯/尾灯/广告牌），算法做了针对性设计但无法保证 100% 准确
